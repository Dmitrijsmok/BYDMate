package android.hardware.bydauto;

/**
 * Compile-only stub of the firmware value carrier; see bydauto-stubs/build.gradle.kts.
 * Only the fields the push listeners read are mirrored.
 */
public class BYDAutoEventValue {
    public int intValue;
    public double doubleValue;
    public byte[] bufferDataValue;
}
